class ApplicationController < ActionController::Base
    # protected
    # # when a user logs in
    # def after_sign_in_path_for(resource_or_scope)
    #   session[:cart_id] = Cart.create.id # get id of row where it's is_default is set as true
    # end
end
